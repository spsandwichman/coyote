STRUCT Foo
    x: INT,
    y: ^Bar,
END

FN bruh(IN f: ^Foo)
    
    x := f^.x // allowed

    y := f^.y // allowed, because y is a pointer

    y1 := y^ // error - Bar is undefined
END

STRUCT Bar
    z: INT,
END

FN lmao(IN f: ^Foo)
    
    x := f^.x

    y := f^.y^ // Bar is defined now
END